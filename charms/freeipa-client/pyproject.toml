[project]
name = "freeipa-client"
version = "0.0"
requires-python = ">=3.12"
dependencies = [
    "ops ~= 2.22",
]

[project.optional-dependencies]
dev = [
    "coverage[toml] ~= 7.8",
    "ops[testing] ~= 2.22",
    "pytest ~= 8.3",
    "pytest-mock ~= 3.14",
]
